---
import type { IHeroData } from "../libs/type.d.ts"

interface Props {
	HeroData :IHeroData
}

const {HeroData} = Astro.props
const {paragraph, image} = HeroData
const {head, subhead, text, btn} = paragraph
---

<section class="h-[80vh] relative mt-24">
  <div class="2xl:max-w-screen-xl xl:max-w-screen-xl lg:max-w-screen-lg md:max-w-screen-md max-w-screen-sm m-auto h-full flex">
    <div class="w-1/2 flex justify-start items-center h-full pr-10">
      <div class="z-10">
        <h1 class="text-[color:var(--accent)] font-medium text-6xl mb-5">{head.first}<span class="block italic">{head.second}</span></h1>
        <p class="lora font-medium italic text-2xl mb-8">{subhead}</p>
        <p class="mb-10">{text}</p>
        <button class="center py-2 px-12 rounded-full bg-[var(--accent)] text-white font-bold">{btn}</button>
      </div>
      <img class="object-cover h-full w-1/2 absolute left-0 z-0"  src={image.main2} alt="">
    </div>
    <div class="w-1/2">
      <img class="object-cover h-full w-1/2 absolute" src={image.main} />
    </div>
  </div>
</section>